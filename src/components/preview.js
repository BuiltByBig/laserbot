import React, { PropTypes } from 'react'

export default React.createClass({

  name: 'Preview',

  propTypes: {
  },

  render() {
    //const {
      //dismissNotification,
      //notifications,
    //} = this.props

    return (
      <p>Gcode preview here...</p>
    )
  },

})
